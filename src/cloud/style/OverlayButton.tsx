import { style } from 'typestyle';
import * as vars from '../../style/vars';
export const OverlayButton = style({
  background: vars.colorDisabled,
  color: vars.colorButton,
  padding: `10px 24px`
});
