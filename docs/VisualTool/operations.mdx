---

name: Query, Mutation, Subscription
menu: Visual Editor
route: /visual/operations

---

# Query, Mutation, Subscription

Query is your most important node. Without it you can't deploy mock backend or real one. 
To create operation node just match one of your type nodes with modifier. 
Remember you can only have on root type ( one Query, one Mutation, one Subscription)

<video
  autoPlay={true}
  controls
  style={{
    maxWidth: '100%'
  }}
  src={require('./querySelect.mp4')}
/>
